#include <iostream>
#include <string>

using namespace std;


int main() {

	short drinks = 4, sandwiches = 7, amount = 3, B;
	_asm
	{
		mov ax, amount;
		mov bx, amount;
		imul ax, drinks;
		imul bx, sandwiches;
		add ax, bx;
		mov B, ax;
	}

	cout << B << endl;
  
  system("pause");
}
